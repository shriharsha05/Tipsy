<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/MaterialBlueColor</item>
        <item name="colorPrimaryDark">@color/MaterialBlue700</item>
        <item name="colorAccent">@color/OrangeAccent400</item>
    </style>

</resources>
